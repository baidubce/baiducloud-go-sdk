Global:
  version: "2.0"
  group_email: bce-sdk@baidu.com
Default:
  profile:
    - build
Profiles:
  - profile:
    name: build
    mode: AGENT
    environment:
      image: DECK_STD_CENTOS7
      tools:
        - go: 1.13.latest
    build:
      command: sh build.sh
    artifacts:
      release: true